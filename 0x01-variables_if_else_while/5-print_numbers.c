#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print alphapet in lowercase
 *
 * Return: Always zero (success)
 */

int main(void)
{
	int n = 0;

	while (n <= 9)
	{
	printf("%i", n);
	n++;
	}
	return (0);
}
